language: node_js
node_js:
  - lts/*
install:
  - yarn install # npm ci
script:
  - yarn docs:build # npm run docs:build
deploy:
  provider: pages
  cleanup: true
  local_dir: docs/.vuepress/dist
  keep_history: true
  github_token:  $GITHUB_TOKEN
  on:
    branch: master

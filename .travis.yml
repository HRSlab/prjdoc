language: node_js
node_js:
  - lts/*
install:
  - yarn install # npm ci
script:
  - yarn docs:build # npm run docs:build
deploy:
  provider: pages
  cleanup: true
  local_dir: docs/.vuepress/dist
  keep_history: true
  github_token:  35c767ba2637733797b1ad54ee8eac052134ffff
  on:
    branch: master
